// Add currently playing games rate games move to completed with rating

import React, { Component } from 'react';

export default class EditGameplay extends Component {
    render() {
        return (
            <div>
                <p>Currently Playing!</p>
            </div>
        )
    }
}